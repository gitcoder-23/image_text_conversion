import 'package:flutter/material.dart';

class ImageTextInvoice extends StatelessWidget {
  const ImageTextInvoice({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
